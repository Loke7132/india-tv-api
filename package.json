{
  "name": "india-tv-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "India-first TV metadata API and public-stream playlists, with dedicated Tamil and Telugu endpoints.",
  "scripts": {
    "build": "node src/build.mjs",
    "test": "node --test",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
